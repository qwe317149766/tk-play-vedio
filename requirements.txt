# ============================================================================
# TK-PLAY-API 项目依赖包
# ============================================================================
# 安装方法: pip install -r requirements.txt
# ============================================================================

# HTTP 请求库
curl-cffi>=0.5.0          # 支持 curl 的 HTTP 客户端，用于 TikTok API 请求
requests>=2.28.0          # 标准 HTTP 请求库
urllib3>=1.26.0           # HTTP 客户端库

# 加密相关
pycryptodome>=3.15.0       # 加密算法库（AES、ARC4 等）
gmssl>=3.2.1              # 国密算法库（SM3 哈希）
ecdsa>=0.18.0             # 椭圆曲线数字签名算法

# Protobuf
protobuf>=6.0.0           # Google Protocol Buffers，用于 TikTok 协议（需要匹配 gencode 版本）

# 数据库
pymysql>=1.0.2            # MySQL 数据库连接库
redis>=4.5.0              # Redis 数据库客户端

# ============================================================================
# 可选依赖（根据需要取消注释）
# ============================================================================

# 配置文件支持（如果使用 YAML 格式配置文件）
# PyYAML>=6.0

# Java 互操作（如果使用 jpype 调用 Java 代码）
# JPype1>=1.4.0

# ============================================================================
# 标准库（Python 内置，无需安装）
# ============================================================================
# asyncio, threading, json, os, sys, time, logging, typing, 
# traceback, contextlib, queue, collections, datetime, etc.

